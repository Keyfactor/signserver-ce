<?xml version="1.0" encoding="UTF-8"?>
<!-- You may freely edit this file. See commented blocks below for -->
<!-- some examples of how to customize the build. -->
<!-- (If you delete it and reopen the project it will be recreated.) -->
<!-- By default, only the Clean and Build commands use this build script. -->
<!-- Commands such as Run, Debug, and Test only use this build script if -->
<!-- the Compile on Save feature is turned off for the project. -->
<!-- You can turn off the Compile on Save (or Deploy on Save) setting -->
<!-- in the project's Project Properties dialog box.-->
<project name="SignServer-ejb-ValidationWS" default="default" basedir="." xmlns:ejbjarproject="http://www.netbeans.org/ns/j2ee-ejbjarproject/3">
    <description>Builds, tests, and runs the project SignServer-ejb-ValidationWS.</description>
    <import file="nbproject/build-impl.xml"/>
    <import file="../modules.xml"/>
    
    <property name="modulename" value="validationws"/>
    <property name="dist-module.available" value="true"/>
    <property name="clean.available" value="true"/>
    <target name="-pre-compile" depends="with.clover"/>

    <!-- Setup APPSRV_HOME etc -->
    <target name="-pre-init" depends="-modules-set-appserver-home"/>
    
    <!-- Target for regenerating the WSDL. -->
    <target name="regenerate-wsdl" depends="wsgen-ValidationWS" description="Regenerates the WSDL from the source.">
        <copy todir="${src.resources.dir}/META-INF/wsdl/" failonerror="true">
            <fileset dir="${build.dir}/generated-sources/jax-ws/resources/">
                <include name="*.wsdl"/>
                <include name="*.xsd"/>
            </fileset>
        </copy>
        <echo>
Remember to update the soap:address location in the WSDL to start with
"https://" for WS to work on JBoss 4.
        </echo>
    </target>
    
    <target name="dist-module" depends="init,-init-macrodef-convertclasspath,dist">
        <copy todir="../../lib/">
            <fileset dir="${dist.dir}">
                <include name="*.jar"/>
            </fileset>
        </copy>
        
        <!-- Create the module descriptor file for this module -->
        <property name="distJar" value="SignServer-ejb-ValidationWS.jar"/>
        <property name="defaultExcludes" value="modules/SignServer-BuildTools/,modules/SignServer-ejb/,lib/ext/ext/,lib/ext/hibernate/,lib/ext/javaee-*/*"/>
        <convertclasspath classpath="${javac.classpath}" id="libsToLib" excludes="${defaultExcludes},modules/"/>
        <convertclasspath classpath="${javac.classpath}" id="modsToLib" excludes="${defaultExcludes},lib/" mapperType="flatten" mapperTo="lib/*"/>
        <propertyfile file="../../mods-available/20_${modulename}.properties">
            <entry key="module.name" value="${modulename}"/>
            <entry key="module.type" value="ejb"/>
            <entry key="module.ejb" value="${distJar}"/>
            <entry key="to.lib" value="${libsToLib},${modsToLib}"/>
            <entry key="to.root" value="lib/${distJar}"/>
            <entry key="postprocess.files" value="postprocess.file1"/>
            <entry key="postprocess.file1.src" value="${distJar}"/>
            <entry key="postprocess.file1.includes" value="META-INF/ejb-jar.xml"/>
        </propertyfile>
    </target>

    <!--
        ===================
        Process ejb-jar.xml
        ===================
    -->
    <!-- <target name="-pre-compile">
        <echo message="Transforming ejb-jar.xml"/>
        <property name="ejbjarheader" value="AUTOGENERATED do not edit. Edit setup/ejb-jar instead !!"/>
        <property name="datasource.jndi-name" value="SignServerDS"/>
        <property name="datasource.jndi-name-prefix" value="java:/"/>
        <copy file="${resource.dir}/ejb-jar.xml" tofile="${meta.inf}/ejb-jar.xml"
            failonerror="true" overwrite="true">
                <filterchain>
                    <expandproperties/>
                </filterchain>
        </copy>
    </target>-->

    <!--

    There exist several targets which are by default empty and which can be 
    used for execution of your tasks. These targets are usually executed 
    before and after some main targets. They are: 

      -pre-init:                 called before initialization of project properties
      -post-init:                called after initialization of project properties
      -pre-compile:              called before javac compilation
      -post-compile:             called after javac compilation
      -pre-compile-single:       called before javac compilation of single file
      -post-compile-single:      called after javac compilation of single file
      -pre-dist:                 called before archive building
      -post-dist:                called after archive building
      -post-clean:               called after cleaning build products
      -pre-run-deploy:           called before deploying
      -post-run-deploy:          called after deploying

    (Targets beginning with '-' are not intended to be called on their own.)

    Example of pluging an obfuscator after the compilation could look like 

        <target name="-post-compile">
            <obfuscate>
                <fileset dir="${build.classes.dir}"/>
            </obfuscate>
        </target>

    For list of available properties check the imported 
    nbproject/build-impl.xml file. 


    Other way how to customize the build is by overriding existing main targets.
    The target of interest are: 

      init-macrodef-javac:    defines macro for javac compilation
      init-macrodef-debug:    defines macro for class debugging
      do-dist:                archive building
      run:                    execution of project 
      javadoc-build:          javadoc generation 

    Example of overriding the target for project execution could look like 

        <target name="run" depends="<PROJNAME>-impl.jar">
            <exec dir="bin" executable="launcher.exe">
                <arg file="${dist.jar}"/>
            </exec>
        </target>

    Notice that overridden target depends on jar target and not only on 
    compile target as regular run target does. Again, for list of available 
    properties which you can use check the target you are overriding in 
    nbproject/build-impl.xml file. 

    -->
</project>
